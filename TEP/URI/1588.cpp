#include <bits/stdc++.h>

using namespace std;

int main(){

    int T;

    cin >> T;

    while(T--){

        int N, M;
        cin >> N >> M;

        while(N--){
            string a;
            map <string,int> time;
            cin >> a;
            time.insert(a);
        }

        while(M--){

        }

    }

    return 0;
}
