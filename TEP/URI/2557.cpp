#include <bits/stdc++.h>

using namespace std;

int main(){

    char *R,*L,*J;
    R = (char *) malloc(sizeof(char)*1000000);
    L = (char *) malloc(sizeof(char)*1000000);
    J = (char *) malloc(sizeof(char)*1000000);

    while(scanf("%s+%s=%s",R,L,J) != EOF)
    {
        int v;
        
        if(R == 'R')
        {

        }
        if(L == 'L')
        {

        }
        if(J == 'J')
        {
            v =
        }
    }

    return 0;
}
